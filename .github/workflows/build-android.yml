name: Build Android APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: 🏗 Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🏗 Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔧 Create .env file
        run: |
          echo "EXPO_PUBLIC_REVENUECAT_API_KEY=${{ secrets.REVENUECAT_API_KEY }}" > .env

      - name: 🔨 Expo Prebuild (generate native Android project)
        run: npx expo prebuild --platform android --clean

      - name: ⚙️ Configure Gradle Properties
        run: |
          echo "org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m" >> android/gradle.properties
          echo "android.lint.abortOnError=false" >> android/gradle.properties
          echo "android.lint.checkReleaseBuilds=false" >> android/gradle.properties

      - name: 🏗 Build Debug APK with Gradle
        working-directory: android
        run: ./gradlew assembleDebug --no-daemon --stacktrace -x lint -x lintDebug

      - name: 📱 Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: brilliant-wellbeing-apk
          path: android/app/build/outputs/apk/debug/*.apk
          retention-days: 30
